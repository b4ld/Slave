export default class Constants {
    static readonly CONTAINER_NAME_PREFIX = 'zentry-server-';

    static readonly CONFIGURATION_FILE = './configuration.xml';

    static readonly LOG_FOLDER = './logs';
}
